/*
L2-003 月饼
分数 25
作者 陈越
单位 浙江大学

月饼是中国人在中秋佳节时吃的一种传统食品，不同地区有许多不同风味的月饼。
现给定所有种类月饼的库存量、总售价、以及市场的最大需求量，请你计算可以获得的最大收益是多少。

注意：销售时允许取出一部分库存。样例给出的情形是这样的：假如我们有 3 种月饼，
其库存量分别为 18、15、10 万吨，总售价分别为 75、72、45 亿元。
如果市场的最大需求量只有 20 万吨，那么我们最大收益策略应该是卖出全部 15 万吨第 2 种月饼、
以及 5 万吨第 3 种月饼，获得 72 + 45/2 = 94.5（亿元）。

输入格式：
每个输入包含一个测试用例。每个测试用例先给出一个不超过 1000 的正整数 N 表示月饼的种类数、
以及不超过 500（以万吨为单位）的正整数 D 表示市场最大需求量。
随后一行给出 N 个正数表示每种月饼的库存量（以万吨为单位）；
最后一行给出 N 个正数表示每种月饼的总售价（以亿元为单位）。数字间以空格分隔。

输出格式：
对每组测试用例，在一行中输出最大收益，以亿元为单位并精确到小数点后 2 位。

输入样例：
3 20
18 15 10
75 72 45

输出样例：
94.50
*/

#include<bits/stdc++.h>
using namespace std;
#define debug(x) cout<<#x<<" "<<x<<"\n"

int N, D;
struct moke{
	double cnt, sale;
	double dan;
};

int main() {
	cin>>N>>D;
	vector<moke> v(N);
	for(int i = 0; i < N; i++) {
		cin>>v[i].cnt;
	}
	for(int i = 0; i < N; i++) {
		cin>>v[i].sale;
		v[i].dan = v[i].sale / v[i].cnt;
	}
	auto cmp = [](const moke& m1, const moke& m2) {
		return m1.dan > m2.dan;	
	};
	sort(v.begin(), v.end(), cmp);
	double sum = 0.0;
	for(int i = 0; i < N; i++) {
		if(D >= v[i].cnt) {
			sum += v[i].sale;
			D -= v[i].cnt;
		} else if(D < v[i].cnt) {
			sum  += v[i].dan * (double)D;
			D = 0; // 清零
			break;
		}
	}
	printf("%.2lf", sum);
	return 0;
}

// 【总结】
// 1) 题型通用模板（分数背包/单位价值贪心）
// - 适用特征：物品可拆分，目标是最大化收益（或最小化成本）。
// - 标准框架：先算单位价值，再按单位价值从大到小排序，依次尽量取满，不够就取部分。
// - 正确性直觉：每次优先拿单位价值最高的部分，局部最优可扩展为整体最优。
// - 复杂度：排序 O(NlogN)，扫描 O(N)，总复杂度 O(NlogN)。
// - 通用易错点：可拆分题才用该贪心；输出精度按题目要求保留小数。
//
// 2) 本题差异化补充
// - 本题输入给的是“库存总量 + 总售价”，所以要先转成单位售价 dan = sale / cnt。
// - 市场需求 D 有上限，循环中当 D 归零就可以提前结束。
// - 最后一种可能只卖部分库存，收益按单位售价乘以剩余需求累加。
