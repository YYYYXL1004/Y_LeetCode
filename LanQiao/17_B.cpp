/*
P16225 [蓝桥杯 2026 省 A] 量子 2048

题目描述：
深蓝色的量子冷冻稀释制冷机正在平稳运行，这里是“国家量子安全实验室”的核心机房。

小蓝作为“量子 2048”战略工程的首席架构师，正盯着屏幕上的一块 2048 x 2048 的量子比特逻辑阵列。
这块阵列是构建超大规模量子计算机的基础单元，每一个比特位点必须被初始化为两种状态之一：L（低能级状态）或 Q（量子激发态）。

为了通过严苛的“国家量子安全实验室”准入审计，阵列的逻辑分布必须严格遵循以下三项由量子纠缠动力学推导出的校验准则：
1. 行量子奇偶校验：阵列中的每一行，处于 Q 状态的比特数量必须为奇数。
2. 列量子奇偶校验：阵列中的每一列，处于 Q 状态的比特数量必须为奇数。
3. 局部纠缠约束校验：阵列中任意一个 2 x 2 的子区域（阵列中行列连续、相邻紧贴的 2 行 2 列网格区块），处于 Q 状态的比特数量必须为奇数。

阵列中哪怕只有一个局部区域不符合准则，整个芯片在超导状态下就会发生量子退相干，导致计算失败。
小蓝想要计算出，在 2048 x 2048 的固定规格下，理论上存在多少种不同的初始化填充方案能够通过审计。

现在，作为小蓝的算法顾问，请你帮助他完成计算。由于方案数可能很大，请将结果对 998244353 取模后输出。

输入格式：
无

输出格式：
这是一道结果填空题，你只需要算出结果后提交即可。
本题的结果为一个整数，在提交答案时只填写这个整数，填写多余的内容将无法得分。

输入输出样例 #1
输入 #1

输出 #1

*/

#include<bits/stdc++.h>
using namespace std;
#define debug(x) cout<<#x<<" "<<x<<"\n"

// 矩阵异或构造题
const int MOD = 998244353;

long long qpow(long long a, long long b) {
	long long res = 1;
	while(b) {
		if(b&1) res = (res * a) % MOD;
		a = (a * a) % MOD;
		b >>= 1;
	}
	return res;
}

int main() {
	cout<<qpow(2, 2*2048-3) % MOD;
	return 0;
}

/*
【总结】
1. 通用模板/万能思路：
   对“行约束 + 列约束 + 每个 2 x 2 小块约束”的 01 矩阵题，常用异或不变量建模，把格子值写成 a[i] xor b[j] xor c 的形式统一处理。
2. 模板适用特征：
   约束都是“奇偶性（mod 2）”条件，且局部 2 x 2 关系会把全局结构压缩成少量自由变量。
3. 标准框架：
   先由 2 x 2 奇偶关系推出矩阵可由首行首列决定；再代入行列奇偶约束得到线性方程组；最后统计自由元个数 k，答案是 2^k（再取模）。
4. 复杂度结论：
   结果填空可直接公式化为 O(1)；一般 n x m 情况可在 O(n + m) 内完成自由度统计。
5. 通用易错点：
   奇偶方程必须在 GF(2) 下推导；指数较大时要用快速幂；最终要按题目模数取模。
6. 本题差异化补充：
   n = m = 2048 为偶数，综合三类奇偶约束后自由度为 2n - 3（此处 n=2048），故方案数为 2^(2*2048-3)。
7. 本题特化处理：
   直接输出 qpow(2, 2*2048-3) mod 998244353，即可得到填空答案。
*/
