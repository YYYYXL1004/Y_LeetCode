/*
P16224 [蓝桥杯 2026 省 A] 均衡数

题目描述：
如果一个正整数的二进制表示中（无前导 0），1 的数量和 0 的数量相同，则我们称其为一个“均衡数”。

现在，请你找到一个均衡数 x，使得 |2026202620262026 - x| 的值最小。
若存在多个均衡数使得 |2026202620262026 - x| 的值相同且最小，则取其中最小的一个。

输入格式：
无

输出格式：
这是一道结果填空的题，你只需要算出结果后提交即可。
本题的结果为一个整数，在提交答案时只填写这个整数，填写多余的内容将无法得分。

输入输出样例 #1
输入 #1

输出 #1

*/

#include<bits/stdc++.h>
using namespace std;
#define debug(x) cout<<#x<<" "<<x<<"\n"



int main() {
	long long n = 2026202620262026;
//	cout<<n;
//	int cnt0 = 0, cnt1 = 0;
//	while(n) {
//		if(n&1) cnt1++;
//		else cnt0++;
//		n >>= 1;
//	}
//	cout<<cnt0<<" "<<cnt1;  // 25,26
	
	// 方法一：
//	long long n1 = 0, n2 = 0; // 局部变量如果不赋初值，系统会分配给它一个内存中残留的垃圾值,必须初始化为0
//	for(long long i = 49; i >= 25; i--) {
//		n1 += 1LL<<i;
//	}
////	cout<<n1<<"\n"; // 1125899873288192
//	for(long long i = 51; i >= 0; i--) {
//		if(i == 51) n2 += 1LL<<i;      // 必须使用1LL，因为默认1是32位int数，左移51位会溢出
//		else if(i <= 24) n2 += 1LL<<i;  
//		else continue;
//	}
////	cout<<n2<<"\n"; // 2251799847239679
	
	// 方法二：
	long long n1 = ((1LL<<25)-1) << 25; // 先搞出25个1,再2左移25位
	long long n2 = (1LL<<51) | ((1LL<<25) - 1); // 1+26个0+25个1
	long long cha1 = n - n1, cha2 = n2 - n;  
	if(cha2 < cha1) cout<<n2<<"\n";
	else cout<<n1<<"\n";
	return 0;
}

/*
【总结】
1. 通用模板/万能思路：
   遇到“与某个固定整数最近、同时满足二进制 0/1 个数约束”的题，先固定二进制总位数，再构造目标数上下两侧最近的合法候选。
2. 模板适用特征：
   约束只和位上 0/1 个数有关，不依赖具体十进制结构；目标是最小绝对差，通常只需比较相邻边界候选。
3. 标准框架：
   先统计目标数位长和 0/1 个数偏差，再构造“小于等于目标的最大合法数”和“大于等于目标的最小合法数”，最后比较距离并按题意处理平局。
4. 复杂度结论：
   位运算直接构造候选，时间复杂度 O(1)，空间复杂度 O(1)。
5. 通用易错点：
   左移高位必须用 1LL，避免 32 位 int 溢出；平局时要返回更小值；构造时注意无前导 0。
6. 本题差异化补充：
   目标数的二进制里 1 比 0 多 1 个（注释中已验证为 25,26），最近均衡数只需看两个边界：
   n1 = 25 个高位 1 + 25 个低位 0，n2 = 1 个最高位 1 + 26 个 0 + 25 个低位 1。
7. 本题特化处理：
   直接公式化构造 n1、n2 后比较 cha1 与 cha2，若距离相同返回 n1（更小），满足题目 tie-break。
*/
