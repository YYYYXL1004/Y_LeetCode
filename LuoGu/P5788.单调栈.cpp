/*
P5788 【模板】单调栈

题目背景
模板题，无背景。

2019.12.12 更新数据，放宽时限，现在不再卡常了。

题目描述
给出项数为 n 的整数数列 a1...an。

定义函数 f(i) 代表数列中第 i 个元素之后第一个大于 ai 的元素下标，
即 f(i)=min{i<j<=n, aj>ai}{j}。若不存在，则 f(i)=0。

请输出 f(1...n)。

输入格式
第一行一个正整数 n。
第二行 n 个正整数 a1...an。

输出格式
一行 n 个整数，依次表示 f(1), f(2), ..., f(n)。

输入样例
5
1 4 2 3 5

输出样例
2 5 4 5 0

数据范围
30%: n<=100
60%: n<=5*10^3
100%: 1<=n<=3*10^6, 1<=ai<=10^9
*/

#include<bits/stdc++.h>
using namespace std;
#define ll long long

int main() {
	ios::sync_with_stdio(false);
	cin.tie(nullptr);

	int n;  cin>>n;
	vector<int> v(n+1);
	vector<int> ans(n+1, 0);  // 也是存下标
	for(int i = 1; i <= n; i++){
		cin>>v[i];
	}
	stack<int> st; // 里面存元素的下标
	for(int i = 1; i<= n; i++) {
		// 1. 结算历史债务：如果栈不为空，且当前新来的 a[i] 破坏了单调递减
		// 也就是说，当前 a[i] 比栈顶下标对应的元素要大
		while(!st.empty() && v[i] > v[st.top()]) {
			ans[st.top()] = i;
			st.pop(); // 出栈
		}
		// 2. 当前元素入栈，作为新的债主，等待未来比它大的元素来结算它
		st.push(i);
	}
	for(int i = 1; i <= n; i++) {
		cout<<ans[i]<<" ";
	}
	return 0;
}

/*
【总结】
1. 通用模板（右侧第一个更大元素）
- 适用信号：需要找每个位置“右边第一个满足比较关系”的下标/值。
- 标准框架：从左到右扫描；栈存下标并维护按值单调不增；新元素能“打败”栈顶就持续弹栈并结算答案。
- 复杂度：每个下标最多入栈一次、出栈一次，总时间 O(n)，空间 O(n)。
- 通用易错点：比较符号要和题意一致（本题是严格大于，用 >）；栈里必须存下标，否则无法回填答案位置。

2. 本题差异化补充
- 题目要输出的是“下标”而不是数值，所以 ans[st.top()] = i。
- 若右侧不存在更大值，答案保持 0，故 ans 初始化为 0 即可。
*/
